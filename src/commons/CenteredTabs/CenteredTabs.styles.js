import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    flexGrow: 1
  },
  section: {
    height: '100%',
    paddingTop: 5
  },
  gridStyle: {
    padding: '0.8em'
  },
});

export { useStyles };
